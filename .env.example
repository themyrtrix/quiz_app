# Shows the required local environment variable without exposing a real secret.

# Local mode is fully offline and stores questions and attempts in .data/quiz-data.json.
DATABASE_MODE="local"

# Optional: set DATABASE_MODE=remote and provide a Prisma Postgres URL to use the hosted database.
# DATABASE_URL="******HOST:PORT/DATABASE?sslmode=require"
